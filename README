This is a console application which was intended to work for both 32 and 64-bit applications. It provides remote and direct memory access to external processes.
When the current mode is DMA, the specified DMA library is injected into the target process in order to execute functions transmitted as queries over UDP.
Communication is established over UDP on ports 69 and 70.
